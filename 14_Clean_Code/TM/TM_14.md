# Tugas Pendahuluan 14 : Clean_Code
Nama: Steven Taufik Fajar 
NIM: 103122400068 
Kelas: SE-08-02 

## Soal

## Program/kode
Dari dua kode di bawah ini, mana yang kamu ingin cari masalahnya dan perbaiki di tengah-tengah malam, katakanlah jam 1 malam? Mengapa?
```
function processUser(user) {
  if (user) {
    if (user.isActive) {
      if (user.hasPermission) {
        return doSomething(user)
      }
    }
  }
  return null
}
```
```
function processUser(user) {
  if (!isValidCandidate(user)) return null;
  return doSomething(user);
}

function isValidCandidate(user) {
  return user && user.isActive && user.hasPermission;
}
```

## Deskripsi
pilih 2 si alasanya ya karena jelas aja codenya di bikin perfunction dari pada yg pertama banyak cabang if nya bikin pusing apalagi jam 1 malam otak udh cape pengen turu

